import codelingo/ast/go

go.file(depth = any):
  go.decls:
    go.func_decl:
      go.block_stmt:
        go.list:
          @playground.highlight
          go.if_stmt:
            go.ident:
              child_count == 0
              type == "bool"
            go.block_stmt:
              child_count == 0

