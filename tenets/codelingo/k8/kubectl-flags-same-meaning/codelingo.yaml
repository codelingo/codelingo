# TODO This tenet is only a mock. The query here needs to be updated.

tenets:
  - name: kubectl-flags-same-meaning
    flows:
      codelingo/docs:
        title: Title in Docs
        body: |
      codelingo/review:
        comment: The flag {{flagAName}} is described as {{flagADescription}}. However, the flag {{flagBName}} is described differently. i.e. {{flagBDescription}}. Please rename flags if they differ semantically.
    query: |
      import codelingo/ast/go
      
      @review comment
      go.func_decl(depth = any):
        go.func_type:
          go.field_list:
            go.field:
              go.names:
                go.ident:
                  type == "bool"
