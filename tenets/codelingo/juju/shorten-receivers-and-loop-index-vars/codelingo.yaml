funcs:
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: shorten-receivers-and-loop-index-vars
    vars:
      pName: "{{ protoName(literalValue) }}"
    flows:
      codelingo/rewrite:
      codelingo/docs:
        title: Shorten Receivers and Loop Index Vars
        body: |
          Receivers and loop index vars can still be very short.
      codelingo/review:
        comment: You can probably shorten this identifiers name.
    query: |
      import codelingo/ast/go
      
      # TODO Write query. This is a stub query.
      @rewrite name
      @review comment
      go.ident
