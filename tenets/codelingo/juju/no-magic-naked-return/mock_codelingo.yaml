# TODO This is only a mock. The query here needs to be updated. Delete this line and rename this file to codelingo.yaml.
funcs:
  - name: appendIntegrationTest
    type: resolver
    body: |
      function (fname) {
        fname.replace(/\.go$/, "")
        fname.replace(/_test$/, "")
        return fname + "_integration_test.go"
      }
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: no-magic-naked-return
    flows:
      codelingo/rewrite:
        name: mycoolkey
      codelingo/docs:
        title: No Magic Naked Return
        body: |
          TODO this needs to be a summary of the tenet, usually taken from the docs
      codelingo/review:
        comment: TODO This is hard to understand for a casual observer because it uses a clever scoping trick. Readability is more important.
    query: |
      import codelingo/ast/go
      
      @rewrite name
      @review comment
      go.func_decl(depth = any):
        go.ident(depth = any)
