funcs:
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: api-not-closed
    vars:
      pName: "{{ protoName(literalValue) }}"
    flows:
      codelingo/rewrite:
      codelingo/docs:
        title: Api Not Closed
        body: |
          If you connect to an API, you also need to close it or pass on the responsibility.
      codelingo/review:
        comment: Either this database needs to be closed here or you need to pass on the responsibility.
    query: |
      import codelingo/ast/go
      
      # TODO Write query. This is a stub query.
      @rewrite name
      @review comment
      go.ident
