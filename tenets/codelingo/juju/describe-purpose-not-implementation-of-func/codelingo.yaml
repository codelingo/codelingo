funcs:
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: describe-purpose-not-implementation-of-func
    vars:
      pName: "{{ protoName(literalValue) }}"
    flows:
      codelingo/rewrite:
      codelingo/docs:
        title: Describe Purpose Not Implementation of Func
        body: |
          Describe the purpose, not the implementation of functions.
      codelingo/review:
        comment: This comment describes the function's implementation, but it should describe its purpose.
    query: |
      import codelingo/ast/go
      import codelingo/ast/nlp
      
      # TODO Write query. This is a stub query.
      @rewrite name
      @review comment
      go.ident
