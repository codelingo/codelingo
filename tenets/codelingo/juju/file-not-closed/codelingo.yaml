funcs:
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: file-not-closed
    vars:
      pName: "{{ protoName(literalValue) }}"
    flows:
      codelingo/rewrite:
      codelingo/docs:
        title: File Not Closed
        body: |
          If you open a file, you also need to close it or pass on the responsibility.
      codelingo/review:
        comment: Either this files needs to be closed here or you need to pass on the responsibility.
    query: |
      import codelingo/ast/go
      
      # TODO Write query. This is a stub query.
      @rewrite name
      @review comment
      go.ident
