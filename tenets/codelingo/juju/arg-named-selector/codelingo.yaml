funcs:
  - name: doesNotMatch
    type: asserter
    body: |
      function (reg, str) {
        return !str.match(reg)
      }
tenets:
  - name: arg-named-selector
    vars:
      pName: "{{ protoName(literalValue) }}"
    flows:
      codelingo/rewrite:
      codelingo/docs:
        title: Arg Named Selector
        body: |
          `Selector` (implies request for several things, possibly to set up a Request?)
      codelingo/review:
        comment: This would be better named `Selector` as it implies that it sets up a request for something.
    query: |
      import codelingo/ast/go
      
      # TODO Write query. This is a stub query.
      @rewrite name
      @review comment
      go.ident
